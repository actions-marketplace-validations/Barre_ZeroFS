import { Guides } from '@/components/Guides'
import { Resources } from '@/components/Resources'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'ZeroFS Documentation',
  description:
    'ZeroFS exposes object storage through filesystem mounts, client libraries, and NBD block devices.',
}

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Resources', id: 'resources' },
]

<HeroPattern />

# ZeroFS Documentation

ZeroFS exposes S3-compatible, Azure Blob Storage, or Google Cloud Storage through filesystem mounts and NBD block devices. Stock NFS and Linux 9P clients need no ZeroFS-specific software. On supported Linux kernels, the [native kernel client](/kernel-client) is the fastest mount option; the bundled FUSE client works across a wider range and mounts without root. The server itself runs in userspace.

<div className="not-prose mt-6 mb-16 flex gap-3">
  <Button href="/quickstart" arrow="right">
    Get Started
  </Button>
  <Button href="/configuration" variant="outline">
    Configuration
  </Button>
</div>

## What ZeroFS does {{ anchor: false }}

ZeroFS keeps filesystem data and metadata in the configured object store and uses local disk only for caching. Every protocol reaches the same filesystem. {{ className: 'lead' }}

- **Filesystem behavior is tested continuously** with pjdfstest, xfstests, crash injection, and model-based simulation. See [Durability and consistency](/durability#continuous-integration-testing).
- **Separate metadata and data paths** handle namespace operations independently from immutable file-data segments.
- **No external database service**; data and metadata live in the object store.
- **Encrypted before upload**: every block is compressed, then encrypted with XChaCha20-Poly1305, with no unencrypted mode.

<div className="not-prose">
  <Button href="/encryption" variant="text" arrow="right">
    Learn about encryption
  </Button>
</div>

<Guides />

<Resources />
