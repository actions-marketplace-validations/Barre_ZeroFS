name: jepsen

on:
  workflow_call:

permissions:
  contents: read

jobs:
  jepsen:
    name: Run jepsen local-fs suite (9P)
    runs-on: ubuntu-latest
    # Backstop only; the per-step timeouts below fire first so the store
    # upload still runs. Without these a wedged trial burns the 6h default.
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Install Leiningen + FUSE
        run: |
          sudo apt-get update
          # gnuplot: jepsen's default perf-graph checker shells out to it to
          # render latency/rate PNGs; without it that checker throws and the
          # verdict drops to :unknown.
          sudo apt-get install -y fuse3 gnuplot
          curl -fsSL https://raw.githubusercontent.com/technomancy/leiningen/stable/bin/lein \
            -o /usr/local/bin/lein
          sudo chmod +x /usr/local/bin/lein
          lein version

      - name: Fetch the prebuilt ZeroFS binary
        uses: actions/download-artifact@v4
        with:
          name: zerofs-ci-bin
          path: zerofs/target/ci
      - run: chmod +x zerofs/target/ci/zerofs

      - name: Start MinIO
        run: |
          docker run -d \
            --name minio \
            -p 9000:9000 \
            -e MINIO_ROOT_USER=minioadmin \
            -e MINIO_ROOT_PASSWORD=minioadmin \
            minio/minio server /data

          # Wait for MinIO to be ready
          for i in {1..30}; do
            if curl -f http://localhost:9000/minio/health/live; then
              echo "MinIO is ready"
              break
            fi
            sleep 1
          done

          # The glue points every run at s3://zerofs-jepsen/run-<uuid>.
          wget https://dl.min.io/client/mc/release/linux-amd64/mc
          chmod +x mc
          ./mc alias set myminio http://localhost:9000 minioadmin minioadmin
          ./mc mb myminio/zerofs-jepsen || true

      - name: Assemble local-fs (upstream @ pin + ZeroFS glue)
        run: |
          # Upstream is not vendored: clone it at the pinned commit and lay the
          # ZeroFS glue on top (jepsen/README.md explains the pieces).
          PIN=0921306efc27d89a72b9041daaf9f854c57ac980
          git clone https://github.com/jepsen-io/local-fs.git /tmp/local-fs
          git -C /tmp/local-fs -c advice.detachedHead=false checkout "$PIN"
          cp jepsen/zerofs.clj /tmp/local-fs/src/jepsen/local_fs/db/zerofs.clj
          cp jepsen/zerofs-ha.clj /tmp/local-fs/src/jepsen/local_fs/db/zerofs_ha.clj
          git -C /tmp/local-fs apply "$GITHUB_WORKSPACE/jepsen/local-fs.patch"
          cp jepsen/run.sh /tmp/local-fs/run.sh
          chmod +x /tmp/local-fs/run.sh

      - name: POSIX conformance (quickcheck)
        working-directory: /tmp/local-fs
        timeout-minutes: 10
        run: |
          # The ZeroFS backend self-manages the server (against the MinIO started
          # above) and the 9P FUSE mount, so no separate mount step is needed
          # here. Each trial is a full setup/teardown; --quickcheck-tests bounds
          # CI time.
          ./run.sh run quickcheck \
            --db zerofs \
            --zerofs-bin "$GITHUB_WORKSPACE/zerofs/target/ci/zerofs" \
            --quickcheck-tests 5 \
            --time-limit 30

      - name: Crash consistency (lose-unfsynced-writes)
        working-directory: /tmp/local-fs
        timeout-minutes: 10
        run: |
          ./run.sh run quickcheck \
            --db zerofs \
            --zerofs-bin "$GITHUB_WORKSPACE/zerofs/target/ci/zerofs" \
            --lose-unfsynced-writes \
            --history-scale 8 \
            --quickcheck-tests 4 \
            --time-limit 30

      - name: HA failover consistency (--db zerofs-ha --failover)
        working-directory: /tmp/local-fs
        timeout-minutes: 10
        run: |
          # A leader + standby cluster where the fault fails the leader over to the
          # standby; the same fs model-checker verifies no write is lost,
          # resurrected, or botched across the failover. A failover is slow (~20s
          # incl restart), so use fewer trials and shorter histories.
          ./run.sh run quickcheck \
            --db zerofs-ha \
            --failover \
            --zerofs-bin "$GITHUB_WORKSPACE/zerofs/target/ci/zerofs" \
            --history-scale 20 \
            --quickcheck-tests 3 \
            --time-limit 60

      - name: Upload jepsen store on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: jepsen-store
          path: /tmp/local-fs/store
          if-no-files-found: ignore
