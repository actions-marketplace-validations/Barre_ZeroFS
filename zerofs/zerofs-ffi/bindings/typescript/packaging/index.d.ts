// Type surface for the loader `index.js`. The runtime resolves the platform
// cdylib and loads it; the public types are exactly the generated module's.
// (Mirrors the generator's own `index.d.ts`, which is `export * from
// "./zerofs_ffi.js"`.)
export * from "./zerofs_ffi.js";
