# Own workspace so the nightly-only libfuzzer dependency never enters the
# stable zerofs workspace build.
[workspace]

[package]
name = "ninep-proto-fuzz"
version = "0.0.0"
publish = false
edition = "2024"

[package.metadata]
cargo-fuzz = true

[dependencies]
bytes = "1"
libfuzzer-sys = "0.4"

[dependencies.ninep-proto]
path = ".."

[[bin]]
name = "decode"
path = "fuzz_targets/decode.rs"
test = false
doc = false
bench = false
